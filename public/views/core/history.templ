package core

import "github.com/condemo/nes-cards/types"

templ HistoryView(gl []types.Game) {
	// TODO: crear tarjetas con los datos de `gl` para mostrar el historial
	<div id="history-view" class="flex flex-col">
		<h1 class="text-4xl">HistoryView</h1>
	</div>
}
